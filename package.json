{
  "name": "groupmarket-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:generate": "npx prisma generate --schema=packages/database/prisma/schema.prisma",
    "db:migrate": "npx prisma db push --schema=packages/database/prisma/schema.prisma",
    "test:ingest": "node --env-file=.env -r ts-node/register scripts/test-ingestion.ts"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "embedded-postgres": "^18.4.0-beta.17",
    "ts-node": "^10.9.2",
    "turbo": "^1.10.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.14"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.22",
    "openai": "^6.38.0",
    "playwright": "^1.60.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  }
}
